# MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/gezi_gunlugu_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=isil
server.port = 8080

# Hibernate / JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Dosya Yükleme
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# Thymeleaf Önbelle?ini Kapat (Geli?tirme yaparken de?i?iklikleri anl?k görmek için)
spring.thymeleaf.cache=false